import { StripRequestContextPipe } from './strip-request-context.pipe';

describe('StripRequestContextPipe', () => {
  it('should be defined', () => {
    expect(new StripRequestContextPipe()).toBeDefined();
  });
});
